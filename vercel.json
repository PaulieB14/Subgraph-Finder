{
  "version": 2,
  "buildCommand": "cd standalone && npm install && npm run build && mkdir -p ../public && cp -r dist/* ../public/ && mkdir -p ../public/dashboard && cp -r dashboard/* ../public/dashboard/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/index.js"
    }
  ]
}
